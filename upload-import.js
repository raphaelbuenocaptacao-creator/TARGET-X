// TARGET X v6 — Planilha oficial como fonte única
(function(){
  if(window.__txOfficialImporterV60)return;window.__txOfficialImporterV60=true;
  const ROWS='tx_official_daily_v60',META='tx_official_meta_v60',BACKUP='tx_official_backup_v60';
  const $=id=>document.getElementById(id),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
  const norm=s=>String(s||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toUpperCase().replace(/[^A-Z0-9]+/g,' ').trim();
  const n=v=>{if(typeof v==='number')return isFinite(v)?v:0;let s=String(v??'').trim().replace(/R\$|\s/g,'');if(!s)return 0;if(s.includes(',')&&s.includes('.'))s=s.replace(/\./g,'').replace(',','.');else if(s.includes(','))s=s.replace(',','.');return Number(s)||0};
  const num=v=>new Intl.NumberFormat('pt-BR').format(Math.round(Number(v)||0));
  const money=v=>new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL',maximumFractionDigits:0}).format(Number(v)||0);
  const css=document.createElement('style');css.textContent=`#txUploadBar{margin:10px 0 2px;display:grid;grid-template-columns:minmax(0,1fr) auto;gap:10px;align-items:center;border-color:#28533d}#txUploadBar .txUpTitle{font-weight:1000;font-size:13px}.txUpStatus{font-size:10px;color:var(--muted);margin-top:4px}#txUploadActions{display:flex;gap:7px;align-items:center;flex-wrap:wrap;justify-content:flex-end}#txSheetFile{display:none}#txChooseSheet{background:#102b1f;border-color:#2a6248;color:#c9ffe1}#txConfirmSheet{background:var(--green);color:#061006;border-color:var(--green)}.txFileName{max-width:240px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;font-size:10px;color:var(--muted)}#txImportPreview{display:none;margin:8px 0 12px;border:1px solid var(--line);border-radius:16px;padding:12px;background:#06100b}.txPreviewGrid{display:grid;grid-template-columns:repeat(6,1fr);gap:7px}.txPV{border:1px solid var(--line);border-radius:10px;padding:8px}.txPV span{display:block;font-size:8px;color:var(--muted);text-transform:uppercase}.txPV b{display:block;margin-top:4px}@media(max-width:900px){.txPreviewGrid{grid-template-columns:repeat(3,1fr)}}@media(max-width:720px){#txUploadBar{grid-template-columns:1fr}#txUploadActions{justify-content:stretch}#txUploadActions button{flex:1}.txFileName{max-width:100%}}@media(max-width:520px){.txPreviewGrid{grid-template-columns:1fr 1fr}}`;document.head.appendChild(css);
  const aliases={'JOSYENE FREITAS':'JOSYENE APARECIDA DE FREITAS','ANA CAROLINE':'ANA CAROLINE DA SILVA LOPES PEREIRA','ADRIANO MOREIRA':'EDSON ADRIANO PINTO MOREIRA','MARCIO ALFAIA':'MARCIO VINICIOS MARTINS ALFAIA','ANDRE CARRICO':'ANDRE LUIS CARRIÇO DOS SANTOS','JESSICA BARBOSA':'JESSICA GONCALVES BARBOSA','MATHEUS DOMINGOS SANTOS':'MATHEUS DOMINGOS SANTOS DE SOUZA','LETICIA LEANDRA':'LETICIA LEANDRA DE TOLEDO','OTAVIO JOSE DE OLIVEIRA MARTIN':'OTAVIO JOSE DE OLIVEIRA MARTINS'};
  let candidate=null;
  function get(row,...names){const ks=Object.keys(row);for(const name of names){const t=norm(name),k=ks.find(k=>norm(k)===t);if(k!==undefined)return row[k]}return''}
  function ymd(v){if(!v)return'';if(typeof v==='number'&&window.XLSX?.SSF?.parse_date_code){const d=XLSX.SSF.parse_date_code(v);if(d)return`${d.y}-${String(d.m).padStart(2,'0')}-${String(d.d).padStart(2,'0')}`}const s=String(v).trim();if(/^\d{4}-\d{2}-\d{2}/.test(s))return s.slice(0,10);const m=s.match(/(\d{1,2})[\/-](\d{1,2})[\/-](\d{4})/);if(m)return`${m[3]}-${m[2].padStart(2,'0')}-${m[1].padStart(2,'0')}`;const d=new Date(s);return isNaN(d)?'':`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`}
  function resolvePerson(raw){const r=norm(raw);if(!r)return'';const ak=Object.keys(aliases).find(k=>norm(k)===r);if(ak)return aliases[ak];const exact=(window.HIST||[]).find(p=>norm(p.name)===r);if(exact)return exact.name;const toks=r.split(' ').filter(x=>x.length>2);let best=null,score=0;(window.HIST||[]).forEach(p=>{const pt=new Set(norm(p.name).split(' ')),hit=toks.filter(t=>pt.has(t)).length,s=hit/Math.max(1,toks.length);if(hit&&s>score){score=s;best=p.name}});return score>=.5?best:String(raw).trim().toUpperCase()}
  function countStatus(status,rx){return String(status||'').split('#').map(norm).filter(x=>rx.test(x)).length}
  function parseRow(row){const date=ymd(get(row,'Data','Data de atendimento')),person=resolvePerson(get(row,'Promotor','Promotor de marketing','Profissional','Captador'));if(!date||!person)return null;const couples=Math.max(1,Math.round(n(get(row,'Casais','Atendimentos')))||1),status=get(row,'Status do contrato','Status contrato'),explicitSales=n(get(row,'Vendas ativas','Vendas Ativas','Vendas')),explicitCancel=n(get(row,'Canceladas','Cancelamentos')),sales=explicitSales||countStatus(status,/^ATIVO\b/),cancelled=explicitCancel||countStatus(status,/^CANCEL/);const qCol=n(get(row,'Q')),nqCol=n(get(row,'NQ')),qual=norm(get(row,'Qualificação','Qualificado','Motivo de qualificação'));let q=qCol,nq=nqCol;if(!q&&!nq&&couples===1){q=qual==='Q'||qual.includes('PERFIL QUALIFICADO')?1:0;nq=qual.startsWith('NQ')?1:0}const general=n(get(row,'VGV bruto','VGV Geral','Valor vendido','VGV')),active=n(get(row,'VGV ativo','VGV Ativo'))||(sales>0?general:0),gift=n(get(row,'Brindes','Valor brindes','Valor em brindes','Valor Brinde')),nt=get(row,'NoTour','No Tour','Descrição de motivo No Tour','Motivo NoTour'),notour=n(nt)||((String(nt).trim()&&couples===1)?1:0);return{date,person,couples,q,nq,sales,cancelled,vgv:active,vgv_general:general,gift,notour,attendances:couples,source:'official-sheet'}}
  function sheetObjects(sheet){const m=XLSX.utils.sheet_to_json(sheet,{header:1,defval:'',raw:true});if(!m.length)return[];let hi=m.findIndex(r=>r.some(c=>/DATA/.test(norm(c)))&&r.some(c=>/PROMOTOR|PROFISSIONAL|CAPTADOR/.test(norm(c))));if(hi<0)hi=0;const h=m[hi].map((x,i)=>String(x||`campo_${i}`).trim()||`campo_${i}`);return m.slice(hi+1).filter(r=>r.some(v=>String(v).trim())).map(r=>Object.fromEntries(h.map((k,i)=>[k,r[i]??''])))}
  function aggregate(rows){const map={};rows.forEach(r=>{const k=r.date+'|'+r.person;if(!map[k])map[k]={...r,id:'sheet-'+k};else Object.keys(blank()).forEach(f=>map[k][f]=(Number(map[k][f])||0)+(Number(r[f])||0))});return Object.values(map).sort((a,b)=>a.date.localeCompare(b.date)||a.person.localeCompare(b.person))}
  function blank(){return{couples:0,q:0,nq:0,sales:0,cancelled:0,vgv:0,vgv_general:0,gift:0,notour:0,attendances:0}}
  function sum(rows){return rows.reduce((a,r)=>{Object.keys(blank()).forEach(k=>a[k]+=Number(r[k])||0);return a},blank())}
  async function ensureXlsx(){if(window.XLSX)return;await new Promise((ok,no)=>{const s=document.createElement('script');s.src='https://cdn.jsdelivr.net/npm/xlsx@0.18.5/dist/xlsx.full.min.js';s.onload=ok;s.onerror=()=>no(new Error('Não consegui carregar o leitor da planilha.'));document.head.appendChild(s)})}
  function showPreview(){const p=$('txImportPreview');if(!candidate||!p)return;const t=candidate.total,m=candidate.meta;p.style.display='block';p.innerHTML=`<div class="label">CONFERÊNCIA ANTES DE APLICAR</div><div class="txPreviewGrid" style="margin-top:8px"><div class="txPV"><span>Registros válidos</span><b>${num(t.couples)}</b></div><div class="txPV"><span>Q / NQ</span><b>${num(t.q)} / ${num(t.nq)}</b></div><div class="txPV"><span>Vendas</span><b>${num(t.sales)}</b></div><div class="txPV"><span>Cancelamentos</span><b>${num(t.cancelled)}</b></div><div class="txPV"><span>VGV ativo</span><b>${money(t.vgv)}</b></div><div class="txPV"><span>VGV geral</span><b>${money(t.vgv_general)}</b></div></div><div class="tiny" style="margin-top:8px">Período: ${m.from.split('-').reverse().join('/')} → ${m.latest.split('-').reverse().join('/')} • ${num(candidate.groups.length)} grupos Data + Profissional • ${num(candidate.ignored)} linhas ignoradas por falta de Data/Profissional.</div>`}
  async function prepare(file){candidate=null;$('txConfirmSheet').disabled=true;$('txUploadStatus').textContent='Lendo e conferindo a planilha...';try{await ensureXlsx();const wb=XLSX.read(await file.arrayBuffer(),{type:'array'});let source=[],seen=0;wb.SheetNames.forEach(sn=>{const o=sheetObjects(wb.Sheets[sn]);seen+=o.length;source.push(...o)});const parsed=source.map(parseRow).filter(Boolean);if(!parsed.length)throw new Error('Não encontrei registros válidos com Data e Profissional/Promotor.');const groups=aggregate(parsed),total=sum(parsed),dates=parsed.map(r=>r.date).sort(),meta={file:file.name,rows:total.couples,groups:groups.length,from:dates[0],latest:dates.at(-1),when:new Date().toISOString()};candidate={groups,total,meta,ignored:Math.max(0,seen-parsed.length)};showPreview();$('txUploadStatus').innerHTML=`<span class="green">✓ Planilha conferida.</span> Revise o resumo abaixo e confirme a atualização.`;$('txConfirmSheet').disabled=false;$('txConfirmSheet').textContent='Confirmar atualização'}catch(e){$('txUploadStatus').innerHTML=`<span class="red">Erro:</span> ${esc(e.message||e)}`;$('txImportPreview').style.display='none'}}
  function confirm(){if(!candidate)return;const oldRows=localStorage.getItem(ROWS),oldMeta=localStorage.getItem(META);localStorage.setItem(BACKUP,JSON.stringify({rows:oldRows,meta:oldMeta,when:new Date().toISOString()}));localStorage.setItem(ROWS,JSON.stringify(candidate.groups));localStorage.setItem(META,JSON.stringify(candidate.meta));$('txUploadStatus').innerHTML=`<span class="green">✓ Base oficial atualizada.</span> ${num(candidate.meta.rows)} registros até ${candidate.meta.latest.split('-').reverse().join('/')}.`;$('txConfirmSheet').textContent='Atualizado ✓';$('txUndoSheet').style.display='inline-block';window.TX?.reloadOfficial?.();setTimeout(()=>{$('txConfirmSheet').textContent='Confirmar atualização';$('txConfirmSheet').disabled=false},1600)}
  function undo(){const b=JSON.parse(localStorage.getItem(BACKUP)||'null');if(!b)return;if(b.rows===null)localStorage.removeItem(ROWS);else localStorage.setItem(ROWS,b.rows);if(b.meta===null)localStorage.removeItem(META);else localStorage.setItem(META,b.meta);localStorage.removeItem(BACKUP);$('txUndoSheet').style.display='none';$('txUploadStatus').textContent='Última atualização desfeita.';window.TX?.reloadOfficial?.()}
  function mount(){if($('txUploadBar'))return;const nav=document.querySelector('.tabs');if(!nav)return;const box=document.createElement('div');box.id='txUploadBar';box.className='card';box.innerHTML=`<div><div class="txUpTitle">📊 Base oficial do TARGET X</div><div class="txUpStatus" id="txUploadStatus">Escolha a planilha. O TARGET X confere antes de substituir a base.</div></div><div id="txUploadActions"><input id="txSheetFile" type="file" accept=".xlsx,.xls,.csv"><button id="txChooseSheet">Escolher planilha</button><span class="txFileName" id="txFileName">Nenhum arquivo</span><button id="txConfirmSheet" class="primary" disabled>Confirmar atualização</button><button id="txUndoSheet" style="display:${localStorage.getItem(BACKUP)?'inline-block':'none'}">Desfazer última</button></div>`;nav.parentNode.insertBefore(box,nav);const prev=document.createElement('div');prev.id='txImportPreview';box.insertAdjacentElement('afterend',prev);$('txChooseSheet').onclick=()=>$('txSheetFile').click();$('txSheetFile').onchange=()=>{const f=$('txSheetFile').files?.[0];$('txFileName').textContent=f?.name||'Nenhum arquivo';if(f)prepare(f)};$('txConfirmSheet').onclick=confirm;$('txUndoSheet').onclick=undo}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',mount);else mount();
})();